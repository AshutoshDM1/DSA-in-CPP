#include<iostream>
using namespace std;


int main () 
{
  // the normal size of int is 4 byts 
  long int num = 0; // it gives int size of 8 byts

  long long int num = 0; // it gives int size of 16 byts {only work in some machine}

  short int num = 0; // it reduce the size of int to 2 byts

  signed int num = 0;// Normal int size does noting

  unsigned int num = 0; // in this only positive integer can be stored also increase the capcity to storge a integer
 
  return 0;
}
